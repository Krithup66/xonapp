# Community Screen Features

หน้าชุมชนได้รับการอัปเดตให้มีฟีเจอร์ล้ำสมัยตาม Flutter design พร้อม UX ที่ดีขึ้น

## 🚀 ฟีเจอร์ที่เพิ่มเข้ามา

### 1. **Swipe-to-Dismiss**
- กวาดนิ้วจากขวาไปซ้ายเพื่อลบการ์ด
- มี haptic feedback เมื่อเริ่มกวาดและเมื่อถึงจุดที่จะลบ
- Animation ที่ลื่นไหลเมื่อลบการ์ด

### 2. **Haptic Feedback**
- Light impact: เมื่อเริ่มกวาดนิ้ว
- Heavy impact: เมื่อถึงจุดที่จะลบ
- Medium impact: เมื่อกดที่การ์ดหรือปุ่มต่างๆ
- Success notification: เมื่อลบการ์ดสำเร็จ

### 3. **Pull-to-Dismiss (Detail Screen)**
- ปัดลงเพื่อปิดหน้าจอรายละเอียด
- Animation ที่ลื่นไหลพร้อม fade effect

### 4. **Parallax Scrolling**
- พื้นหลังเลื่อนช้ากว่าเนื้อหาหลัก
- สร้างมิติความลึกให้กับ UI

### 5. **Tab Navigation**
- Tab "Feed" และ "Discover"
- Active state พร้อม animation

### 6. **Enhanced Topic Circles**
- รองรับ active/inactive state
- Icons และสีที่แตกต่างกันตาม state

## 📦 การติดตั้งแพ็กเกจ

รันคำสั่งต่อไปนี้เพื่อติดตั้งแพ็กเกจที่จำเป็น:

```bash
cd mobile-app
npm install expo-haptics react-native-gesture-handler react-native-reanimated
```

หรือใช้ yarn:

```bash
cd mobile-app
yarn add expo-haptics react-native-gesture-handler react-native-reanimated
```

## ⚙️ การตั้งค่า

### สำหรับ iOS
ไม่ต้องตั้งค่าเพิ่มเติม - Expo จัดการให้แล้ว

### สำหรับ Android
เพิ่มโค้ดต่อไปนี้ใน `android/app/src/main/java/.../MainActivity.java`:

```java
import com.swmansion.gesturehandler.react.RNGestureHandlerEnabledRootView;

public class MainActivity extends ReactActivity {
  @Override
  protected ReactActivityDelegate createReactActivityDelegate() {
    return new ReactActivityDelegate(this, getMainComponentName()) {
      @Override
      protected ReactRootView createRootView() {
        return new RNGestureHandlerEnabledRootView(MainActivity.this);
      }
    };
  }
}
```

## 🎨 Design Updates

### สีใหม่
- Background: `#0F111A` (Deep Space Blue)
- Active Tab: `#00B0FF` (Blue Accent)
- Card Background: Glassmorphism with opacity

### Layout
- Bento Grid: 2 columns
- Aspect Ratio: 0.75 (ตาม Flutter design)
- Spacing: 15px between cards

## 📱 การใช้งาน

1. **ลบการ์ด**: กวาดนิ้วจากขวาไปซ้ายที่การ์ด
2. **ดูรายละเอียด**: กดที่การ์ดเพื่อเปิดหน้าจอรายละเอียด
3. **ปิดรายละเอียด**: ปัดลงที่หน้าจอรายละเอียด
4. **เปลี่ยน Tab**: กดที่ "Feed" หรือ "Discover"
5. **เลือก Topic**: กดที่วงกลม topic ด้านบน

## 🔧 Troubleshooting

### Haptic Feedback ไม่ทำงาน
- ตรวจสอบว่าได้ติดตั้ง `expo-haptics` แล้ว
- บน iOS Simulator อาจไม่ทำงาน (ต้องทดสอบบนอุปกรณ์จริง)

### Swipe Gesture ไม่ทำงาน
- ตรวจสอบว่าได้ติดตั้ง `react-native-gesture-handler` แล้ว
- ตรวจสอบการตั้งค่า Android (ดูด้านบน)

### Animation ไม่ลื่นไหล
- ตรวจสอบว่าได้ติดตั้ง `react-native-reanimated` แล้ว
- ตรวจสอบว่าใช้ `useNativeDriver: true` ใน animations

## 📝 Notes

- ฟีเจอร์ทั้งหมดใช้ native drivers สำหรับ performance ที่ดีที่สุด
- Haptic feedback ทำงานได้ดีที่สุดบนอุปกรณ์จริง
- Parallax effect จะเห็นชัดเจนเมื่อ scroll ช้าๆ
